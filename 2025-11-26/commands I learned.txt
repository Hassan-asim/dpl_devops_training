1: ctrl-L ( clear screen)
2: ctrl-D ( exit the current shell)
3: ctrl-Z ( puts the current process into suspended background)
4: ctrl-C ( kills the current process )
5: ctrl-H ( works the same as baclkspace)
6: ctrl-A ( goes to the beginning of the line)
7: ctrl-W ( delets the word before the cursor)
8: ctrl-U ( delets from the biginning of line to cursor position)
9: ctrl-E ( goes to the end of line)
10: Tab   ( auto compleates file, directories and binaries)
11: chown (used to change user owenrship of a file or diroctory)
12: chgrp ( used to change group ownership )
13: chmod (used to change the permission on the file)
14: chmod [0,1,2,3,4,5,6,7]=num1num2num3 filename ( different number represent diff permission  eg 0 meand no permission , and teh sum of r ,w , x numbers will give the value where r is 4 , w is 2 and x is 1 so if we want all permission we will write chmod 777 filename since num1=4+2+1=7 for user, num2=4+2+1=7 for group , and num3=4+2+1=7 for other )
15: sudo chown new_owner_name filename ( eg sudo chown root file1 it will changethe ownership of file1 to root )
16: sudo chgrp new_group_name filename (eg sudo chgrp bin file1)
17: cat filename (create thefile and opens it )
18: cat file1 file2 (concatinate multipal files and display the output i.e the entire content of first file is followed by that of the second file)
19: cat file1 file2 > newfile ( combine multiple files and saves the output into a new file)
20: cat file >> existingfile ( appends the file to the end of the existing file )
21: cat > file ( any subsequent lines typed will go into the file , until cntrl+d is typed)
22: cat >> file (any subsequent lines are appended to the file , until cntrl+d is typed)
23: less filename ( display the content of large file screen by screen )
24: cat filename |less (display the content of large file screen by screen )
25: head -n numoflines path/filename (it will display the first 10 lines of a file by default or you can specify teh numoflines in the command   or simply use head -<no of lines> filename)
26: tail -n numoflines path/filename (it will display the last 10 lines of a file by default or you can specify teh numoflines in the command   or simply use tail -<no of lines> filename)
27: sed -e command <filename> (specify editing commands at the command line , operate on file and put the output on standard out )
28: sed -f scriptfile <filename> (specify a scriptfile containing sed command , operate on file and put output on standard out )
29: echo "i hate you" | sed s/hate/love/  ( use sed to filter standard input , putting output on standard output)
30: sed s/current_string/replacement_string/ file ( substitute first string occurrence in every line)
31: sed s/current_string/replacement_string/g file (substitute all string occurrence in every line)
32: sed 1,3s/current_string/replacement_string/g file (substitute all string occurrence in a range of line)
33: sed -i s/current_string/replacement_string/g file (save changes for string substitution in the same file)
34: awk '{ print $0 }' /etc/passwd  (prints the entire file)
35: awk -f: '{ print $1 }' /etc/passwd  (prints the first column of every line , seprated by a colon)
36: awk -f: '{ print $1 $7}' /etc/passwd  (prints the first and seven field of every line)
37: sort file ( sort the lines in the file according to the characters at the beginning of each line )
38: cat file1 file 2 | sort ( combine two files , then sort the lines and display the output on the terminal )
39: sort -r filename ( sort the lines in the reverse order) 
40: sort -k 3 filename (sort the lines by the 3rd field on each line instead on the beginning 
41: sort -u file1 file2 > file3 ( it will sort the content of file1 and file2 into the file3 but also -u command will remove the duplicate concurrent lines in file3 after sort)
42: uniq -c filename ( to count the number of duplicate entries in a file )
43: paste file1 file2 ( to paste the content from 2 files using the default delimiters )
44: paste -d, file1 file2 ( to paste the content from 2 files using a different delimiters in this case ,)
45: join file1 file2 (its an enhanced version of paste it joins the 2 files using a common column)
46: split largfile splitcommonname ( eg split bill splitbill it will split the bill in 1000 lines sections and saved it as splitbill<suffix> , suppose the bill have more then 20k lines so the split command will make 20 different files called splitbilla, splitbillb..etc)
47: commands to use with any file editor like find , grep ,vi to search for specific string  eg in a string  " i am very good at cooking food " the gerp g... will display good, and find a. |oo. will display am, at,ood,ook,ood , simillearly using the ..$ will display the last 2 charectors eg od, , using c.* will display the cooking food , and using c.*g will display the cooking , i.* will display the full string) 
48: grep [pattern] <filename> ( search for pattern in a file and print all matching lines)
49: grep -v [pattern] <filename> ( print all lines that do not match the pattern )
50: grep [0-9] <filenmame> (print the lines containing ther numbers 0 through 9)
51: grep -c 3 [pattern] <filename> (print context of lines (specify number of lines above and below the pattern) for matching the pattern. here, the number of line is specified as 3 )
52: strings book1.xls | grep my_string (it will search and print all lines of the file or files that have the my_string 
53: tr [option] set1 [set2] (used for changing text utilities in a file)
54: cat filename | tr a-z A_Z ( it wil change all the cheracters in thefile from lower case to uppercase)
55: tr '{}' '()' inputfile outputfile ( translate braces into pranthesis)
56: echo " this is for testing" | tr [:space:] '\t'  ( translate white space into tabs)
57: echo " this    is    for    testing" | tr -s [:space:]   ( squeeze repititioon of cheracters using -s)
58: echo " this is for testing" | tr -d 't'  (delete specified cheracters using -d option)
59: echo "my username is hassan 337" |tr -cd [:digit:] ( compliment the sets using -c option)
60: tr -cd [:print:] < file.txt ( remove all non-printable cheracters from a file )
61: tr -s '\n' ' ' < file.txt ( joins all lines in a file into a single line)
62: tee ( to save the output of a command in a file eg ls -l | tee outputfile 
and then to see the content of file use cat outputfile)
63:wc file ( display the number of line, bytes , and words   but if we want a specific option we can use -h, -c, -w   for number of line, bytes , and word respectively)
64:cut ( used to manipulate column based files designed to extract different columns)
65:ls -l | cut -d" " -f3 ( this will display the 3rd column seprate by blank space )
66:curl url ( it will read and display the content of the mentened url page )
67:curl -o saved.html  url  (it will save the fetched text from the url in a file named in this case saved.html )
68:ssh systemname ( to login to a remote system)
69:ssh someone@someSystem ( to login as a different user in a system remotely)
70:scp <localfile> user@remotesystem :/home/user/ (to copy a local file to a remote system )




